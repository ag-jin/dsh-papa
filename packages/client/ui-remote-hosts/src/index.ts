/** Host loader entry for the remote-host switcher's browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the remote-host switcher. */
export function apply(): void {}
